apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: traefik-dashboard
  namespace: traefik
spec:
  secretName: traefik-dashboard-tls
  commonName: traefik.kentaro1043.internal
  dnsNames:
    - traefik.kentaro1043.internal
  issuerRef:
    name: homelab-ca
    kind: ClusterIssuer
