apiVersion: v1
kind: Service
metadata:
  name: ai
  namespace: ai
  labels:
    app: ai
spec:
  type: ClusterIP
  selector:
    app: ai
  ports:
    - name: dummy
      port: 8080
      targetPort: 8080
