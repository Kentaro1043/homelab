apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: misskey-postgres
  namespace: misskey
spec:
  instances: 1
  storage:
    size: 5Gi
    storageClass: nfs-homelab
  bootstrap:
    initdb:
      database: misskey
      owner: misskey
