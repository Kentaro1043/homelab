apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: pds

resources:
  - namespace.yaml
  - statefulset.yaml
  - service.yaml
  - ingress.yaml
  - secrets/pds-secrets.enc.yaml
  - secrets/smtp-credentials.enc.yaml
