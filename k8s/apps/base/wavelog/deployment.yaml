apiVersion: apps/v1
kind: Deployment
metadata:
  name: wavelog
  namespace: wavelog
  labels:
    app.kubernetes.io/name: wavelog
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: wavelog
  template:
    metadata:
      labels:
        app.kubernetes.io/name: wavelog
    spec:
      automountServiceAccountToken: false
      enableServiceLinks: false
      initContainers:
        - name: init-permissions
          image: docker.io/library/busybox:1.37.0
          command:
            - /bin/sh
            - -ec
            - |
              chown -R 33:33 /wavelog/config /wavelog/uploads /wavelog/userdata
              chmod -R u+rwX,g+rwX /wavelog/config /wavelog/uploads /wavelog/userdata
          resources:
            requests:
              cpu: 10m
              memory: 32Mi
            limits:
              cpu: 50m
              memory: 64Mi
          volumeMounts:
            - name: config
              mountPath: /wavelog/config
            - name: uploads
              mountPath: /wavelog/uploads
            - name: userdata
              mountPath: /wavelog/userdata
      containers:
        - name: wavelog
          image: ghcr.io/wavelog/wavelog:3.0.0
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          env:
            - name: CI_ENV
              value: docker
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 500m
              memory: 512Mi
          startupProbe:
            tcpSocket:
              port: http
            failureThreshold: 60
            periodSeconds: 5
          livenessProbe:
            tcpSocket:
              port: http
            periodSeconds: 30
            timeoutSeconds: 5
            failureThreshold: 3
          readinessProbe:
            tcpSocket:
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
            failureThreshold: 3
          volumeMounts:
            - name: config
              mountPath: /var/www/html/application/config/docker
            - name: uploads
              mountPath: /var/www/html/uploads
            - name: userdata
              mountPath: /var/www/html/userdata
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: wavelog-config
        - name: uploads
          persistentVolumeClaim:
            claimName: wavelog-uploads
        - name: userdata
          persistentVolumeClaim:
            claimName: wavelog-userdata
