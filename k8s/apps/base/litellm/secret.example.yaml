# このファイルを直接適用せず、READMEの手順で暗号化する。
apiVersion: v1
kind: Secret
metadata:
  name: litellm-secrets
  namespace: litellm
type: Opaque
stringData:
  LITELLM_MASTER_KEY: REPLACE_WITH_RANDOM_SK_KEY
  GEMINI_API_KEY: REPLACE_WITH_GOOGLE_AI_STUDIO_API_KEY
